# Development Dockerfile: runs ts-node-dev with hot reload via mounted source.
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
COPY prisma ./prisma
RUN npm ci && npx prisma generate
EXPOSE 5000
CMD ["npm", "run", "dev"]
